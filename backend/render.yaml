services:
  - type: web
    name: backend-iaesen-final
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
    disk:
      name: audios_storage
      mountPath: /opt/render/project/src/backend/audios
      sizeGB: 1